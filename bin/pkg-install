#!/bin/env bash

if [[ "$1" == "--bin" ]] && [[ "$#" == "2" ]]; then
    shift
    if hash "$1" 2>/dev/null; then
        exit 0
    else
        step "installing $1"
    fi
fi

sudo dnf install -y "$@" > /dev/null
